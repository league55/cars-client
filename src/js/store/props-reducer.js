const initialState = {
    id: 0,
    host: ""
};

export const PropsConstants = {

};


export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

